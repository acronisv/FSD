mixin item(title, desc)
    .item
        h2.item-title= title
        p.item-desc= desc

mixin colors(title, desc)
    .colors__item-description
        h2.color__item-heading= title
        p.color__item-text= desc