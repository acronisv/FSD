include ../utils/mixins
block variables

doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    title #{title}
    meta(name="description", content=description)
  body(class=pageClass)
    .wrapper
        include ../includes/modules/header
        .content-wrapper
            h1 Pug - HTML templating engine
            block content
        include ../includes/modules/footer
